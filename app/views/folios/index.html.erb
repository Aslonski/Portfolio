<h1> Folio Items</h1>

<% @folio_items.each do |item| %>
  <p><%= item.title%></p>
  <p><%= item.subtitle%></p>
  <p><%= item.body%></p>
  <%= image_tag item.thumb_image if !item.thumb_image.nil?%>
<% end %>